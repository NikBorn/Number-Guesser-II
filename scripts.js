
var rightNumber = correctNumber();
var guessedNumber = document.querySelector()

function correctNumber() {
  var randomNum = Math.floor(Math.random() * 200 +1);
  var newNumber=parseInt(randomNum);
  return newNumber;
}
